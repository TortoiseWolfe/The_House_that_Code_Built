services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ../_svg_assets:/app/_svg_assets
      - ../.env:/app/.env
      - ./templates:/app/templates
    env_file:
      - ../.env
    restart: unless-stopped
